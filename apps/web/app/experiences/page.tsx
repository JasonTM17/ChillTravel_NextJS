import { FeatureOverview } from "@/components/feature-overview";

export default function Page() {
  return (
    <FeatureOverview
      eyebrow="Local experiences"
      title="Food walks, viewpoints, and cultural routes"
      summary="Experience cards are designed for guide profiles, local food walks, sunrise viewpoints, cultural routes, and QR ticket mock flows."
      destinationOffset={10}
    />
  );
}
