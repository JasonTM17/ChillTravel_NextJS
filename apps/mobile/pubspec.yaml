name: vietwander_ai
description: VIETWANDER AI Flutter mobile app with local-first travel intelligence.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  dio: ^5.9.0
  go_router: ^17.0.0
  drift: ^2.30.0
  flutter_secure_storage: ^10.0.0
  flutter_local_notifications: ^19.5.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
